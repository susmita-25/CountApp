// import {CounterComponent} from "../components/CounterComponent";
import useCounter from '../hooks/useCounter';

test("Initial test", () => {
    expect(2).toEqual(2);
    let counter = 2;
    useCounter    
 });